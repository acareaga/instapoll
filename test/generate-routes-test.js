var generatePoll = require('../lib/generate-routes');
var assert = require('chai').assert;

describe('Generate Routes', function() {

  xit('creates valid route', function() {
    assert.typeOf(poll, 'hash');
  });
});
